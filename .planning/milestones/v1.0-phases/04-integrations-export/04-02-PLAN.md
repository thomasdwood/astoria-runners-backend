---
phase: 04-integrations-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/routes.ts
  - src/validation/routes.ts
  - src/services/routeService.ts
  - src/services/meetupExportService.ts
  - src/routes/events.ts
  - client/src/types/index.ts
  - client/src/components/events/meetup-export-popover.tsx
  - client/src/pages/admin/events-page.tsx
  - client/src/components/routes/route-form.tsx
  - client/src/hooks/use-events.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-04]

must_haves:
  truths:
    - "System generates Meetup event description with all variables (route name, distance, category, locations, Strava link, notes)"
    - "Template supports both plain text and HTML Meetup formatting"
    - "Organizer can copy generated description to clipboard with one click"
    - "Organizer can mark event as posted to Meetup via manual checkbox"
    - "Meetup badge visible on event rows for posted events"
    - "Routes have optional stravaUrl field for Strava route links"
    - "Both one-off events and recurring instances can export Meetup descriptions"
  artifacts:
    - path: "src/services/meetupExportService.ts"
      provides: "Meetup description generation with plain text and HTML formats"
      contains: "generateMeetupDescription"
    - path: "client/src/components/events/meetup-export-popover.tsx"
      provides: "Popover UI with format toggle, copy button, and posted checkbox"
      contains: "MeetupExportPopover"
    - path: "src/db/schema/routes.ts"
      provides: "stravaUrl column on routes table"
      contains: "stravaUrl"
  key_links:
    - from: "client/src/components/events/meetup-export-popover.tsx"
      to: "/api/events/:id/meetup-description"
      via: "API call with format query param"
      pattern: "meetup-description"
    - from: "client/src/pages/admin/events-page.tsx"
      to: "client/src/components/events/meetup-export-popover.tsx"
      via: "Popover on event rows"
      pattern: "MeetupExportPopover"
    - from: "src/services/meetupExportService.ts"
      to: "src/db/schema/routes.ts"
      via: "stravaUrl field in route data"
      pattern: "stravaUrl"
---

<objective>
Update Meetup export with new data model, add stravaUrl to routes, add HTML format option, and convert Dialog to Popover UI.

Purpose: Meetup export service exists but uses the old data model (category as string, no startLocation, 'TBD' placeholder for Strava). Need to update for Phase 3.1 changes, add stravaUrl field to routes, support both plain text and HTML formats, convert the full-screen Dialog to an inline Popover, and add Meetup badge on posted events.

Output: Working Meetup export with format toggle, copy-to-clipboard, manual posted checkbox, and Meetup badge on event rows.
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrations-export/04-CONTEXT.md
@.planning/phases/04-integrations-export/04-RESEARCH.md
@src/services/meetupExportService.ts
@src/db/schema/routes.ts
@src/validation/routes.ts
@src/services/routeService.ts
@src/routes/events.ts
@client/src/types/index.ts
@client/src/components/events/meetup-description-dialog.tsx
@client/src/pages/admin/events-page.tsx
@client/src/hooks/use-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stravaUrl to routes and update Meetup export service with format support</name>
  <files>src/db/schema/routes.ts, src/validation/routes.ts, src/services/routeService.ts, src/services/meetupExportService.ts, src/routes/events.ts, client/src/types/index.ts</files>
  <action>
**1. Add stravaUrl to routes schema** (`src/db/schema/routes.ts`):
- Add `stravaUrl: varchar('strava_url', { length: 500 })` as nullable/optional column after endLocation
- This is a URL field where organizers paste Strava route links

**2. Update route validation** (`src/validation/routes.ts`):
- Add `stravaUrl: z.string().url('Must be a valid URL').max(500).optional().or(z.literal(''))` to both createRouteSchema and updateRouteSchema

**3. Update route service** (`src/services/routeService.ts`):
- In `createRoute`: include `stravaUrl: data.stravaUrl || null` in insert values
- In `updateRoute`: add `if (fields.stravaUrl !== undefined) { updateFields.stravaUrl = fields.stravaUrl || null; }` to the update fields builder

**4. Update frontend Route type** (`client/src/types/index.ts`):
- Add `stravaUrl: string | null;` to the Route interface

**5. Update Meetup export service** (`src/services/meetupExportService.ts`):
Rewrite to support the new data model and format toggle:

- Update `EventForMeetup` interface to match current relational query shape:
```typescript
interface EventForMeetup {
  startDateTime: Date;
  startLocation: string | null;
  endLocation: string | null;
  notes: string | null;
  route: {
    name: string;
    distance: string;
    stravaUrl: string | null;
    startLocation: string | null;
    endLocation: string | null;
    category: {
      id: number;
      name: string;
      color: string;
      icon: string;
    };
  };
}
```

- Update `generateMeetupDescription` to accept a `format` parameter ('plain' | 'html'), defaulting to 'plain':
  - **Plain text format**: Keep similar to current but use route.category.name, use actual stravaUrl (not 'TBD'), include startLocation conditionally, determine endLocation as event.endLocation || route.endLocation
  - **HTML format**: Wrap in basic HTML suitable for Meetup.com — `<p>` tags, `<b>` for labels, `<a href>` for Strava link, `<br>` for line breaks. Per CONTEXT.md, this is fixed (not admin-customizable).
  - Include variables: route name, distance, category name, start location (conditional — only if different from default), end location (conditional — only if different from start), Strava route link (only if present), notes, date/time

**6. Update meetup-description endpoint** (`src/routes/events.ts`):
- Add `format` query parameter to `GET /:id/meetup-description`: extract `req.query.format` (default 'plain'), pass to `generateMeetupDescription(event as any, format)`
- Add a Zod schema for the query: `z.object({ format: z.enum(['plain', 'html']).default('plain') })`
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -30 — TypeScript compiles without errors across all modified files</verify>
  <done>Routes have stravaUrl field. Meetup export service generates descriptions in both plain text and HTML formats using the current data model with category objects and conditional locations. API endpoint accepts format query parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Create MeetupExportPopover and update events page with Meetup badge</name>
  <files>client/src/components/events/meetup-export-popover.tsx, client/src/pages/admin/events-page.tsx, client/src/components/events/meetup-description-dialog.tsx, client/src/hooks/use-events.ts</files>
  <action>
**1. Create MeetupExportPopover component** (`client/src/components/events/meetup-export-popover.tsx`):

Build a Popover component (using @radix-ui/react-popover via shadcn's Popover) that replaces the existing Dialog:

```typescript
interface MeetupExportPopoverProps {
  eventId: number;          // DB event ID for API call
  postedToMeetup: boolean;  // Current posted status
  onTogglePosted: () => void; // Callback to toggle posted status
  // For virtual recurring instances (no DB ID):
  calendarEvent?: CalendarEvent; // If provided, generate description client-side
}
```

Popover content:
- **Format toggle**: Two small buttons or a segmented control — "Plain Text" | "HTML" (default: plain text)
- **Description area**: Pre-formatted text area showing generated description
  - For events with DB ID: fetch from `/api/events/${eventId}/meetup-description?format=${format}` on open
  - For virtual recurring instances (calendarEvent provided, no eventId): generate description client-side using the CalendarEvent data — format a simple template with title, date/time, locations, notes
- **Copy button**: Full-width button with Copy icon. On click: `navigator.clipboard.writeText(description)`, show "Copied!" state for 2 seconds
- **Posted to Meetup checkbox**: Manual checkbox with label "Posted to Meetup". Checked state from `postedToMeetup` prop. On change: call `onTogglePosted()`. Only show for DB events (not virtual instances).

Styling: Max width ~400px, padding consistent with other popovers in the app.

**2. Update events-page.tsx** (`client/src/pages/admin/events-page.tsx`):

- Remove import of `MeetupDescriptionDialog`
- Import `MeetupExportPopover` from the new component
- Remove the `meetupEventId` state variable and the `<MeetupDescriptionDialog>` at the bottom

- **For one-off event rows**: Replace the FileText button + separate Dialog with the MeetupExportPopover as a Popover trigger button:
  ```tsx
  <MeetupExportPopover
    eventId={ev.id}
    postedToMeetup={ev.postedToMeetup}
    onTogglePosted={() => handleMeetupToggle(ev)}
  />
  ```
  - The popover trigger should be a Button with a FileText icon (same as current), labeled "Meetup"

- **For one-off event rows**: Remove the separate Switch for postedToMeetup (it moves into the Popover)

- **Add Meetup badge**: In the "Meetup" column cell for one-off events, show a small "Meetup" Badge (variant="secondary" with a green dot or checkmark) if `ev.postedToMeetup` is true. If false, show the MeetupExportPopover trigger button.

- **For recurring instance rows**: Add a MeetupExportPopover trigger in the Meetup column (currently shows dash "—"). Pass `calendarEvent={ce}` prop for client-side description generation. No posted checkbox for virtual instances (they have no DB record to track status).

- **For cancelled instance rows**: Keep showing dash "—" in the Meetup column (no export for cancelled events).

**3. Update hooks** (`client/src/hooks/use-events.ts`):
- The existing `useUpdateMeetupStatus` and `useMeetupDescription` hooks should remain.
- If `useMeetupDescription` doesn't support the format parameter, update it to accept `format: 'plain' | 'html'` and include in the query string.

**4. Delete old dialog** (`client/src/components/events/meetup-description-dialog.tsx`):
- Delete this file entirely — replaced by the new MeetupExportPopover.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -30 — TypeScript compiles without errors. Also verify the old dialog file is deleted: `ls client/src/components/events/meetup-description-dialog.tsx` should fail (file not found).</verify>
  <done>Events page uses Popover instead of Dialog for Meetup export. Format toggle allows plain text or HTML. Copy button works. Manual posted checkbox in Popover. Meetup badge visible on posted events. Recurring instances can generate descriptions client-side. Old dialog deleted.</done>
</task>

</tasks>

<verification>
- [ ] TypeScript compiles without errors: `npx tsc --noEmit`
- [ ] Routes schema includes stravaUrl column
- [ ] Route validation accepts stravaUrl as optional URL
- [ ] Meetup description includes all required variables
- [ ] Format toggle produces both plain text and HTML output
- [ ] MeetupExportPopover replaces old Dialog
- [ ] One-click copy to clipboard works
- [ ] Manual posted checkbox in Popover
- [ ] Meetup badge shows on posted event rows
- [ ] Recurring instances can export Meetup descriptions (client-side)
- [ ] Old meetup-description-dialog.tsx deleted
</verification>

<success_criteria>
- Meetup descriptions include: route name, distance, category, start location (conditional), end location (conditional), Strava link (if present), notes, date/time
- Format toggle in Popover: plain text and Meetup HTML
- One-click copy to clipboard
- Manual "posted to Meetup" checkbox (not auto-checked on copy)
- "Meetup" badge on posted event rows for quick visual scan
- Both one-off and recurring instances can export (recurring via client-side generation)
- stravaUrl available in route forms and included in templates when present
</success_criteria>

<output>
After completion, create `.planning/phases/04-integrations-export/04-02-SUMMARY.md`
</output>
