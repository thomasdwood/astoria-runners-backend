---
phase: 03.1-refinements-missing-features
plan: 06
type: execute
wave: 4
depends_on: ["03.1-04"]
files_modified:
  - client/src/pages/admin/events-page.tsx
  - client/src/components/events/event-form.tsx
  - client/src/components/calendar/month-grid.tsx
  - client/src/components/calendar/list-view.tsx
  - client/src/components/calendar/event-popover.tsx
  - client/src/hooks/use-calendar.ts
autonomous: true
requirements: [recurring-instances]

must_haves:
  truths:
    - "Admin events page shows recurring instances mixed in with one-off events for next 8 weeks"
    - "Recurring instances have a visual badge/icon distinguishing them from one-off events"
    - "Admin can cancel a recurring instance (stays visible as cancelled on public calendar)"
    - "Admin can delete a recurring instance (removed entirely from all views)"
    - "Admin can edit a single recurring instance (materializes as one-off event exception)"
    - "Public calendar shows cancelled instances as greyed out/strikethrough"
    - "Conflict detection warns when creating a one-off event that overlaps a recurring instance"
  artifacts:
    - path: "client/src/pages/admin/events-page.tsx"
      provides: "Events page with mixed one-off and recurring instances"
    - path: "client/src/components/calendar/month-grid.tsx"
      provides: "Month grid with cancelled instance visual treatment"
    - path: "client/src/components/calendar/list-view.tsx"
      provides: "List view with cancelled instance visual treatment"
  key_links:
    - from: "client/src/pages/admin/events-page.tsx"
      to: "client/src/hooks/use-calendar.ts"
      via: "fetch recurring instances for 8-week range"
      pattern: "useCalendar|recurring"
    - from: "client/src/pages/admin/events-page.tsx"
      to: "client/src/hooks/use-events.ts"
      via: "cancel/delete instance mutations"
      pattern: "useCreateEvent|useCancelInstance"
---

<objective>
Add recurring instances to admin events page with cancel/delete/edit actions, display cancelled instances on public calendar, and implement conflict detection when creating events.

Purpose: Gives admins full visibility of upcoming schedule (one-off + recurring in one view) and control over individual recurring instances.
Output: Updated events page, cancel/delete/edit recurring instance UI, public calendar cancelled display, conflict detection.
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-refinements-missing-features/03.1-CONTEXT.md
@.planning/phases/03.1-refinements-missing-features/03.1-RESEARCH.md
@.planning/phases/03.1-refinements-missing-features/03.1-04-SUMMARY.md
@client/src/pages/admin/events-page.tsx
@client/src/hooks/use-events.ts
@client/src/hooks/use-calendar.ts
@client/src/components/calendar/month-grid.tsx
@client/src/components/calendar/list-view.tsx
@client/src/components/calendar/event-popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show recurring instances on admin events page with cancel/delete/edit actions</name>
  <files>
    client/src/pages/admin/events-page.tsx
    client/src/hooks/use-events.ts
  </files>
  <action>
    1. Update `client/src/pages/admin/events-page.tsx`:
       - Fetch both one-off events (existing useEvents()) AND upcoming recurring instances:
         - Use useCalendar() hook (or create a new useUpcomingInstances hook) with view='list', start=now, end=8 weeks from now
         - The calendar list API already returns merged one-off and recurring events
         - Filter the response to separate: DB events (have id) and recurring-only instances (id is null, isRecurring is true)
       - Merge and display in a single list, sorted by startDateTime ascending
       - For each recurring instance, add a visual badge: use lucide-react Repeat icon or a small "Recurring" badge next to the event name
       - For each recurring instance row, show three action buttons:
         - **Edit** (pencil icon): Opens the event form dialog pre-filled with the instance's data (routeId, startDateTime, endLocation, notes, startLocation). On save, creates a new one-off event with `recurringTemplateId` set to link back to the template. This materializes the exception.
         - **Cancel** (X circle icon): Shows AlertDialog "Cancel this instance? It will appear greyed out on the public calendar." On confirm, creates a new event with `recurringTemplateId`, `isCancelled: true`, the instance's startDateTime and routeId. Toast: "Instance cancelled"
         - **Delete** (trash icon): Shows AlertDialog "Delete this instance? It will be removed entirely from the calendar." On confirm, calls PUT /api/recurring-templates/:id/exclude-date with the instance date. Toast: "Instance removed"
       - For cancelled instances already in the list (isCancelled=true DB events), show them with reduced opacity and "Cancelled" badge. Only show a "Restore" action (delete the cancellation event).
       - Keep existing one-off event actions (edit, delete) unchanged

    2. Update `client/src/hooks/use-events.ts`:
       - Add `useCancelRecurringInstance()` mutation: POST /api/events with `{ routeId, startDateTime, recurringTemplateId, isCancelled: true }`
       - Add `useExcludeRecurringDate()` mutation: PUT /api/recurring-templates/:templateId/exclude-date with `{ date: ISO string }`. Invalidates ['calendar'] and ['events'] queries.
       - Add `useRestoreCancelledInstance()` mutation: DELETE /api/events/:id (the cancellation event). Invalidates queries.
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && cd client && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify events page shows recurring instances with Repeat badge and 3 action buttons</manual>
  </verify>
  <done>
    - Events page shows mixed one-off and recurring instances for next 8 weeks
    - Recurring instances have visual Repeat icon badge
    - Cancel action creates isCancelled event
    - Delete action adds date to template excludedDates
    - Edit action opens form pre-filled, creates materialized exception on save
    - Cancelled instances show with reduced opacity and "Cancelled" badge
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Public calendar cancelled display and conflict detection dialog</name>
  <files>
    client/src/components/calendar/month-grid.tsx
    client/src/components/calendar/list-view.tsx
    client/src/components/calendar/event-popover.tsx
    client/src/components/events/event-form.tsx
  </files>
  <action>
    1. Update `client/src/components/calendar/month-grid.tsx`:
       - When rendering events in the month grid day cells, check `event.isCancelled`
       - Cancelled events: render with `opacity-50 line-through` CSS classes on the event text
       - Cancelled events: append "(Cancelled)" text or show a small cancelled indicator
       - Cancelled events should still be clickable to show the popover with "This event has been cancelled" message

    2. Update `client/src/components/calendar/list-view.tsx`:
       - Same treatment as month grid: cancelled events show with `opacity-50 line-through`
       - Add "Cancelled" badge (using the Badge component with a destructive/muted variant)
       - Keep the event visible in the chronological list but clearly marked as cancelled

    3. Update `client/src/components/calendar/event-popover.tsx`:
       - If `event.isCancelled` is true, show a banner at top: "This event has been cancelled" with a warning/muted style
       - Still display the event details (route, time, location) for reference
       - Don't show any action links for cancelled events on the public calendar

    4. Update `client/src/components/events/event-form.tsx` — add conflict detection:
       - When the admin selects a date and time for a new one-off event, check for conflicts with recurring instances:
         - Define time-of-day windows: Morning = before 12:00, Afternoon = 12:00-17:00, Evening = 17:00+
         - After date+time selection, query the calendar for that date (use useCalendar with a single-day range or filter existing data)
         - If a recurring instance exists on the same date in the same time-of-day window, show an AlertDialog:
           Title: "Schedule Conflict"
           Body: "A recurring event ({route name}) is already scheduled for {time} on this date."
           Actions: "Replace" (default — deletes the recurring instance by adding to excludedDates) | "Keep Both" (just save the new event without removing the instance)
         - "Replace" calls the exclude-date mutation THEN creates the new event
         - "Keep Both" just creates the new event
       - Only check conflicts for NEW events (not when editing existing ones)
       - Conflict check should be lightweight — use already-fetched calendar data if available, or make a quick API call
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && cd client && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify cancelled events show greyed/strikethrough on public calendar, conflict dialog appears on overlap</manual>
  </verify>
  <done>
    - Month grid shows cancelled events with opacity-50 and line-through
    - List view shows cancelled events with "Cancelled" badge
    - Event popover shows cancellation banner
    - Event form detects conflicts with recurring instances in same time-of-day window
    - Conflict dialog offers "Replace" (default) and "Keep Both" options
    - Replace deletes the recurring instance before creating the new event
    - TypeScript compiles
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes with zero errors
- Admin events page shows both one-off and recurring events in unified list
- Cancel/delete/edit actions for recurring instances work correctly
- Public calendar renders cancelled events with distinct visual treatment
- Conflict detection dialog appears when scheduling conflicts exist
</verification>

<success_criteria>
- Admin has full control over individual recurring instances
- Public calendar clearly shows cancelled events
- Conflict detection prevents accidental double-scheduling
- All recurring instance management follows the CONTEXT decisions exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-refinements-missing-features/03.1-06-SUMMARY.md`
</output>
