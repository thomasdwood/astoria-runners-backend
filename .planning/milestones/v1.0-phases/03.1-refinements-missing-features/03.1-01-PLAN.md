---
phase: 03.1-refinements-missing-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/categories.ts
  - src/db/schema/settings.ts
  - src/db/schema/routes.ts
  - src/db/schema/events.ts
  - src/db/schema/recurringTemplates.ts
  - src/db/schema/index.ts
  - src/db/seed.ts
autonomous: true
requirements: [categories, start-location, recurring-instances, complex-patterns]

must_haves:
  truths:
    - "Categories table exists with name, color, icon columns and seed data for 4 existing categories"
    - "Settings table exists as a key-value store with default_start_location seeded"
    - "Routes table uses categoryId FK instead of pgEnum category column"
    - "Events table has startLocation and isCancelled columns"
    - "Recurring templates table has frequency, interval, bySetPos, endDate, startLocation, excludedDates columns"
    - "Schema index re-exports all new tables"
  artifacts:
    - path: "src/db/schema/categories.ts"
      provides: "Categories table definition"
      contains: "categories"
    - path: "src/db/schema/settings.ts"
      provides: "Settings key-value table definition"
      contains: "settings"
    - path: "src/db/schema/routes.ts"
      provides: "Updated routes schema with categoryId FK and startLocation"
      contains: "categoryId"
    - path: "src/db/schema/events.ts"
      provides: "Updated events schema with startLocation and isCancelled"
      contains: "isCancelled"
    - path: "src/db/schema/recurringTemplates.ts"
      provides: "Updated recurring templates with frequency, interval, bySetPos, endDate, startLocation"
      contains: "frequency"
  key_links:
    - from: "src/db/schema/routes.ts"
      to: "src/db/schema/categories.ts"
      via: "FK reference"
      pattern: "references.*categories\\.id"
---

<objective>
Create the database foundation for Phase 3.1: new categories and settings tables, migrate routes from pgEnum to FK, add startLocation and isCancelled to events, extend recurring templates for complex patterns.

Purpose: All four feature areas (categories, start location, recurring instances, complex patterns) require schema changes. These must be in place before any service or API work.
Output: Updated Drizzle schema files, new tables, updated seed script.
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-refinements-missing-features/03.1-CONTEXT.md
@.planning/phases/03.1-refinements-missing-features/03.1-RESEARCH.md
@src/db/schema/routes.ts
@src/db/schema/events.ts
@src/db/schema/recurringTemplates.ts
@src/db/schema/index.ts
@src/db/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create categories and settings tables, update all existing schemas</name>
  <files>
    src/db/schema/categories.ts
    src/db/schema/settings.ts
    src/db/schema/routes.ts
    src/db/schema/events.ts
    src/db/schema/recurringTemplates.ts
    src/db/schema/index.ts
  </files>
  <action>
    1. Create `src/db/schema/categories.ts`:
       - `categories` pgTable with: id (serial PK), name (varchar 100, notNull, unique), color (varchar 50, notNull — stores Tailwind color name like "amber"), icon (varchar 50, notNull — stores emoji like "🍺"), createdAt (timestamptz, defaultNow), updatedAt (timestamptz, defaultNow)
       - Export `Category` and `NewCategory` types via InferSelectModel/InferInsertModel

    2. Create `src/db/schema/settings.ts`:
       - `settings` pgTable with: id (serial PK), key (varchar 100, notNull, unique), value (text, notNull), updatedAt (timestamptz, defaultNow)
       - Export `Setting` type via InferSelectModel

    3. Update `src/db/schema/routes.ts`:
       - Remove `routeCategory` pgEnum export entirely
       - Remove the `category` column that uses the enum
       - Add `categoryId: integer('category_id').notNull().references(() => categories.id)` — import categories from './categories'
       - Add `startLocation: varchar('start_location', { length: 200 })` — nullable, uses default setting if null
       - Change `endLocation` from `.notNull()` to nullable (remove the `.notNull()` chain)
       - Add `routesRelations` using drizzle-orm `relations()` with a `one(categories, ...)` relationship
       - Keep all other columns (id, name, distance, version, timestamps) unchanged

    4. Update `src/db/schema/events.ts`:
       - Add `startLocation: varchar('start_location', { length: 200 })` — nullable
       - Add `isCancelled: boolean('is_cancelled').notNull().default(false)` — for cancelled recurring instances
       - Keep all existing columns and relations unchanged

    5. Update `src/db/schema/recurringTemplates.ts`:
       - Add `frequency: varchar('frequency', { length: 20 }).notNull().default('weekly')` — values: 'weekly', 'biweekly', 'monthly'
       - Add `interval: integer('interval').notNull().default(1)` — 2 for biweekly
       - Add `bySetPos: integer('by_set_pos')` — nullable, used for monthly nth-weekday (1=first, 2=second, -1=last)
       - Add `endDate: timestamp('end_date', { withTimezone: true })` — nullable, for optional end date
       - Add `startLocation: varchar('start_location', { length: 200 })` — nullable
       - Add `excludedDates: text('excluded_dates')` — nullable, stores JSON array of ISO date strings like '["2026-03-04","2026-03-11"]' for deleted recurring instance dates
       - Keep all existing columns unchanged

    6. Update `src/db/schema/index.ts`:
       - Add `export * from './categories';`
       - Add `export * from './settings';`
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify all schema files export correct types and tables</manual>
  </verify>
  <done>
    - categories table defined with name/color/icon columns
    - settings table defined as key-value store
    - routes.ts uses categoryId FK instead of pgEnum, has startLocation, endLocation is nullable
    - events.ts has startLocation and isCancelled
    - recurringTemplates.ts has frequency/interval/bySetPos/endDate/startLocation/excludedDates
    - All new schemas re-exported from index.ts
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update seed script with categories, settings, and RRULE migration</name>
  <files>
    src/db/seed.ts
  </files>
  <action>
    1. Import new schema tables: `categories`, `settings`
    2. Add category seed data BEFORE route seeding (categories must exist for FK):
       - { name: 'Brewery Run', color: 'amber', icon: '🍺' }
       - { name: 'Coffee Run', color: 'orange', icon: '☕' }
       - { name: 'Brunch Run', color: 'emerald', icon: '🥂' }
       - { name: 'Weekend', color: 'blue', icon: '🌅' }
       - Use upsert pattern: `onConflictDoUpdate({ target: categories.name, set: { color, icon, updatedAt: sql\`NOW()\` } })`
    3. Add default start location setting seed:
       - key: 'default_start_location', value: 'Astoria Park Track'
       - Use upsert pattern: `onConflictDoUpdate({ target: settings.key, set: { value, updatedAt: sql\`NOW()\` } })`
    4. Update route seed data:
       - Replace `category: 'Brewery Run'` with `categoryId: 1` (or look up the category ID dynamically after inserting categories)
       - Better approach: After upserting categories, query them to get their IDs, then use those IDs in route seed data
       - Add `startLocation: null` (uses default setting)
       - Remove `.notNull()` expectation from endLocation in seed data if it was required before
    5. Add RRULE migration step (after template seeding):
       - Query all existing recurring templates
       - For each template with `;COUNT=` in the rrule string, strip the COUNT parameter: `rrule.replace(/;COUNT=\d+/g, '')`
       - Update the template's rrule in the database
       - Also set `frequency: 'weekly'` and `interval: 1` on all existing templates (they're all simple weekly)
    6. Update recurring template seed data to include `frequency: 'weekly'`, `interval: 1`, `bySetPos: null`, `endDate: null`
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Inspect seed.ts for correct ordering: categories → settings → routes → events → recurring templates → RRULE migration</manual>
  </verify>
  <done>
    - Seed script creates 4 categories with correct colors and icons
    - Seed script creates default_start_location setting with "Astoria Park Track"
    - Route seed data uses categoryId FK instead of category string
    - Existing RRULE strings have COUNT stripped during migration
    - Existing recurring templates get frequency='weekly', interval=1
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All schema files export correct table definitions and types
- Seed script compiles and has correct ordering (categories before routes)
- No references to old `routeCategory` pgEnum remain in schema files
</verification>

<success_criteria>
- Database schema fully prepared for all Phase 3.1 features
- Categories table replaces pgEnum approach
- Settings table provides key-value configuration store
- All four feature areas have their required columns added
- Seed script updated to populate new tables and migrate existing data
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-refinements-missing-features/03.1-01-SUMMARY.md`
</output>
