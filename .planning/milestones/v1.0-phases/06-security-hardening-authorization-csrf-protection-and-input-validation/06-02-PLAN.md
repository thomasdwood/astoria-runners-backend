---
phase: 06-security-hardening-authorization-csrf-protection-and-input-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/events.ts
  - src/routes/events.ts
  - src/routes/routes.ts
  - src/routes/recurringTemplates.ts
  - src/routes/categories.ts
  - src/routes/hosts.ts
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - "POST /api/events with notes over 2000 chars returns 422"
    - "PUT /api/events/0 returns 400 (id=0 rejected, not passed to DB)"
    - "PUT /api/events/-1 returns 400 (negative ids rejected)"
    - "notes max(2000) enforced on create and update for both events and recurring templates"
    - "All existing valid requests continue to succeed (no regression)"
  artifacts:
    - path: "src/validation/events.ts"
      provides: "notes fields in all four schemas updated with max(2000) constraint"
    - path: "src/routes/events.ts"
      provides: "All isNaN(id) guards updated to isNaN(id) || id <= 0"
    - path: "src/routes/routes.ts"
      provides: "All isNaN(id) guards updated to isNaN(id) || id <= 0"
    - path: "src/routes/recurringTemplates.ts"
      provides: "All isNaN(id) guards updated to isNaN(id) || id <= 0"
    - path: "src/routes/categories.ts"
      provides: "All isNaN(id) guards updated to isNaN(id) || id <= 0"
    - path: "src/routes/hosts.ts"
      provides: "All isNaN(id) guards updated to isNaN(id) || id <= 0"
  key_links:
    - from: "src/routes/events.ts"
      to: "src/validation/events.ts"
      via: "createEventSchema, updateEventSchema, createRecurringTemplateSchema, updateRecurringTemplateSchema"
      pattern: "notes.*max.*2000"
---

<objective>
Close two input validation gaps identified in SECURITY.md: unbounded notes strings and integer ID params that allow 0 and negative values.

Purpose: The `notes` field on events and recurring templates has no max length — unbounded user input can cause excessive DB writes. The `isNaN(id)` guard pattern used in all route files rejects NaN but allows 0 and negative integers to reach DB queries (returning 404 but masking misconfigured callers). SECURITY.md explicitly requires `isNaN(id) || id <= 0`.
Output: Updated src/validation/events.ts (4 schema changes), updated 5 route files (17 guard sites)
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@SECURITY.md
@.planning/phases/06-security-hardening-authorization-csrf-protection-and-input-validation/06-RESEARCH.md

<interfaces>
<!-- From src/validation/events.ts — four notes fields to add max(2000) to: -->
```typescript
// createEventSchema — line 8:
notes: z.string().nullable().optional(),

// updateEventSchema — line 20:
notes: z.string().optional(),

// createRecurringTemplateSchema — line 39:
notes: z.string().nullable().optional(),

// updateRecurringTemplateSchema — line 81:
notes: z.string().nullable().optional(),
```

<!-- Current isNaN pattern in all route files (17 occurrences total): -->
```typescript
const id = parseInt(Array.isArray(req.params.id) ? req.params.id[0] : req.params.id, 10);
if (isNaN(id)) {
  res.status(400).json({ error: 'Invalid id' });
  return;
}
```

<!-- Target pattern per SECURITY.md: -->
```typescript
if (isNaN(id) || id <= 0) {
  res.status(400).json({ error: 'Invalid id' });
  return;
}
```

<!-- Count of isNaN(id) occurrences per file (from audit): -->
// events.ts: 8 occurrences (lines 77, 104, 128, 157, 185, 209, 246, and one more)
// routes.ts: 3 occurrences
// recurringTemplates.ts: 5 occurrences
// categories.ts: 2 occurrences
// hosts.ts: 2 occurrences
// Total: ~20 occurrences
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add max(2000) to notes fields in all four event/template schemas</name>
  <files>src/validation/events.ts</files>
  <action>
    Update src/validation/events.ts — add `.max(2000, 'Notes must be 2000 characters or less')` to the `notes` field in four schemas. No other changes.

    Exact replacements:

    In `createEventSchema`:
    - Before: `notes: z.string().nullable().optional(),`
    - After:  `notes: z.string().max(2000, 'Notes must be 2000 characters or less').nullable().optional(),`

    In `updateEventSchema`:
    - Before: `notes: z.string().optional(),`
    - After:  `notes: z.string().max(2000, 'Notes must be 2000 characters or less').optional(),`

    In `createRecurringTemplateSchema`:
    - Before: `notes: z.string().nullable().optional(),`
    - After:  `notes: z.string().max(2000, 'Notes must be 2000 characters or less').nullable().optional(),`

    In `updateRecurringTemplateSchema`:
    - Before: `notes: z.string().nullable().optional(),`
    - After:  `notes: z.string().max(2000, 'Notes must be 2000 characters or less').nullable().optional(),`

    All other fields in all schemas are unchanged. Do not touch startLocation, endLocation, meetupUrl, or any other field.

    The .max() call must be placed BEFORE .nullable().optional() in the chain — Zod processes validators left to right, and max() on a nullable would not apply to null values (which is correct — null bypasses the string length check).
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All four notes fields have max(2000) constraint. TypeScript compiles with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Harden isNaN(id) guards to reject id <= 0 across all five route files</name>
  <files>src/routes/events.ts, src/routes/routes.ts, src/routes/recurringTemplates.ts, src/routes/categories.ts, src/routes/hosts.ts</files>
  <action>
    In each of the five route files, find every occurrence of:

    ```typescript
    if (isNaN(id)) {
    ```

    And replace with:

    ```typescript
    if (isNaN(id) || id <= 0) {
    ```

    The surrounding code (the parseInt line before and the res.status(400) line after) remains unchanged. Only the condition itself changes.

    Do this for ALL occurrences in each file — do not miss any. The grep audit found approximately:
    - src/routes/events.ts: ~8 occurrences
    - src/routes/routes.ts: 3 occurrences
    - src/routes/recurringTemplates.ts: 5 occurrences
    - src/routes/categories.ts: 2 occurrences
    - src/routes/hosts.ts: 2 occurrences

    After editing, verify no `if (isNaN(id)) {` pattern remains in any of the five files (without `|| id <= 0`).

    Do NOT change any other logic in these files — auth middleware, Zod validation, service calls, response shapes are all unchanged.
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && grep -rn "if (isNaN(id)) {" src/routes/ && echo "FAIL: unreplaced guards found" || echo "PASS: all guards updated"; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    No bare `if (isNaN(id)) {` patterns remain in src/routes/ — all updated to `isNaN(id) || id <= 0`.
    TypeScript compiles with no new errors.
    Manual verification (requires running dev server):
    - `curl -X PUT http://localhost:3000/api/events/0 -H "Content-Type: application/json" -d '{"version":0}' -b "sid=VALID_SESSION"` → 400 { error: 'Invalid id' }
    - `curl -X PUT http://localhost:3000/api/events/-5 -H "Content-Type: application/json" -d '{"version":0}' -b "sid=VALID_SESSION"` → 400 { error: 'Invalid id' }
  </done>
</task>

</tasks>

<verification>
After both tasks: `grep -rn "if (isNaN(id)) {" src/routes/` returns no results. TypeScript compiles clean. The four notes Zod schemas have `.max(2000)` in their chains.
</verification>

<success_criteria>
- src/validation/events.ts: all four notes fields have .max(2000, ...) constraint
- All 5 route files: every isNaN(id) check also includes || id <= 0
- npx tsc --noEmit passes with no new errors
- PUT /api/events/0 returns 400, not 404
- POST /api/events with notes > 2000 chars returns 422
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-hardening-authorization-csrf-protection-and-input-validation/06-02-SUMMARY.md`
</output>
