---
phase: 06-security-hardening-authorization-csrf-protection-and-input-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/settings.ts
  - src/routes/settings.ts
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "PUT /api/settings with an unknown key returns 400"
    - "PUT /api/settings/default_start_location with value over 200 chars returns 422"
    - "PUT /api/settings/discord_notifications_enabled with value 'maybe' returns 422"
    - "PUT /api/settings/meetup_description_template with value up to 5000 chars returns 200"
    - "Valid settings writes still succeed (no regression)"
  artifacts:
    - path: "src/validation/settings.ts"
      provides: "EDITABLE_SETTINGS const, EditableSettingKey type, settingValueSchemas map"
      exports: ["EDITABLE_SETTINGS", "EditableSettingKey", "settingValueSchemas"]
    - path: "src/routes/settings.ts"
      provides: "PUT /:key handler with allowlist + per-key validation enforced"
  key_links:
    - from: "src/routes/settings.ts"
      to: "src/validation/settings.ts"
      via: "import { EDITABLE_SETTINGS, EditableSettingKey, settingValueSchemas }"
      pattern: "EDITABLE_SETTINGS\\.includes"
    - from: "src/routes/settings.ts"
      to: "src/services/settingsService.ts"
      via: "settingsService.upsertSetting(key, parsed.data)"
      pattern: "upsertSetting"
---

<objective>
Harden the settings PUT endpoint against arbitrary key writes (P0 gap from SECURITY.md).

Purpose: Any authenticated session can currently write any key to the settings table — including keys the frontend doesn't use — potentially poisoning app configuration. Adding the EDITABLE_SETTINGS allowlist and per-key Zod value constraints closes this gap.
Output: src/validation/settings.ts (new), updated src/routes/settings.ts
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-hardening-authorization-csrf-protection-and-input-validation/06-RESEARCH.md
@SECURITY.md

<interfaces>
<!-- Existing settings route — to be replaced in Task 2 -->
<!-- From src/routes/settings.ts (current PUT handler, lines 36-56): -->
```typescript
router.put(
  '/:key',
  requireAuth,
  asyncHandler(async (req, res) => {
    const key = Array.isArray(req.params.key) ? req.params.key[0] : req.params.key;
    if (!key) {
      res.status(400).json({ error: 'Setting key is required' });
      return;
    }
    const { value } = req.body;
    if (typeof value !== 'string') {
      res.status(422).json({ error: 'Value must be a string' });
      return;
    }
    const setting = await settingsService.upsertSetting(key, value);
    res.status(200).json({ setting });
  })
);
```

<!-- From src/services/settingsService.ts — interface to call: -->
```typescript
export async function upsertSetting(key: string, value: string): Promise<Setting>;
```

<!-- Known editable keys from RESEARCH.md: -->
const EDITABLE_SETTINGS = [
  'default_start_location',       // max 200 chars, non-empty
  'meetup_description_template',  // max 5000 chars (template is larger), can be empty
  'discord_notifications_enabled', // enum: 'true' | 'false'
] as const;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/validation/settings.ts with EDITABLE_SETTINGS allowlist and per-key schemas</name>
  <files>src/validation/settings.ts</files>
  <behavior>
    - EDITABLE_SETTINGS array is exported as a const tuple of the three known keys
    - EditableSettingKey type is inferred from the const array
    - settingValueSchemas maps each key to a Zod schema enforcing per-key constraints:
      - default_start_location: z.string().min(1).max(200)
      - meetup_description_template: z.string().min(0).max(5000) (larger — it's a template)
      - discord_notifications_enabled: z.enum(['true', 'false'])
    - All three exports are named exports (not default)
    - File uses .js extension in imports (ESM project)
  </behavior>
  <action>
    Create src/validation/settings.ts as a new file. This file holds the settings security contracts — keep it separate from the route file to avoid circular dependencies and allow isolated testing.

    Export:
    1. `EDITABLE_SETTINGS` — typed const array `['default_start_location', 'meetup_description_template', 'discord_notifications_enabled'] as const`
    2. `EditableSettingKey` — type alias `typeof EDITABLE_SETTINGS[number]`
    3. `settingValueSchemas` — `Record<EditableSettingKey, z.ZodType<string>>` with per-key schemas:
       - `default_start_location`: `z.string().min(1, 'Value is required').max(200, 'Must be 200 characters or less')`
       - `meetup_description_template`: `z.string().max(5000, 'Must be 5000 characters or less')` (no min — empty template is valid)
       - `discord_notifications_enabled`: `z.enum(['true', 'false'] as [string, ...string[]])` — use this form to satisfy `ZodType<string>` constraint

    Note: Use `z.enum(['true', 'false'] as [string, ...string[]])` to work around TypeScript's strict enum inference when assigned to `ZodType<string>`. Alternatively cast the entire Record to `Record<EditableSettingKey, z.ZodType<string>>`.

    Do NOT export a default — named exports only.
    Do NOT add any import other than `{ z } from 'zod'`.
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>src/validation/settings.ts exists, exports EDITABLE_SETTINGS, EditableSettingKey, settingValueSchemas; TypeScript compiles with no new errors</done>
</task>

<task type="auto">
  <name>Task 2: Update src/routes/settings.ts PUT handler to enforce allowlist and per-key validation</name>
  <files>src/routes/settings.ts</files>
  <action>
    Replace the existing PUT /:key handler body with the hardened version. Keep the router structure and existing GET handlers untouched.

    New PUT handler logic (in order):
    1. Extract key: `const key = Array.isArray(req.params.key) ? req.params.key[0] : req.params.key;`
    2. Allowlist check: `if (!EDITABLE_SETTINGS.includes(key as EditableSettingKey))` → `res.status(400).json({ error: 'Unknown setting key' }); return;`
    3. Per-key schema lookup: `const valueSchema = settingValueSchemas[key as EditableSettingKey];`
    4. Validate value: `const parsed = valueSchema.safeParse(req.body.value);` — if `!parsed.success` → `res.status(422).json({ error: 'Validation failed', details: parsed.error.issues.map((e) => ({ field: 'value', message: e.message })) }); return;`
    5. Upsert: `const setting = await settingsService.upsertSetting(key, parsed.data);` → `res.status(200).json({ setting });`

    Add import at top of file: `import { EDITABLE_SETTINGS, EditableSettingKey, settingValueSchemas } from '../validation/settings.js';`

    Remove the old `typeof value !== 'string'` check — it is replaced by the Zod schema validation.

    The GET / and GET /locations handlers are NOT changed.

    Per SECURITY.md: the allowlist must be a hardcoded constant (imported from validation layer), not fetched from DB.
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles clean. Manual verification (requires running dev server):
    - `curl -X PUT http://localhost:3000/api/settings/unknown_key -H "Content-Type: application/json" -d '{"value":"test"}' -b "sid=VALID_SESSION"` → 400 { error: 'Unknown setting key' }
    - `curl -X PUT http://localhost:3000/api/settings/default_start_location -H "Content-Type: application/json" -d '{"value":"AAAA...201chars"}' -b "sid=VALID_SESSION"` → 422 { error: 'Validation failed', details: [...] }
    - `curl -X PUT http://localhost:3000/api/settings/discord_notifications_enabled -H "Content-Type: application/json" -d '{"value":"maybe"}' -b "sid=VALID_SESSION"` → 422
    - `curl -X PUT http://localhost:3000/api/settings/discord_notifications_enabled -H "Content-Type: application/json" -d '{"value":"true"}' -b "sid=VALID_SESSION"` → 200
  </done>
</task>

</tasks>

<verification>
TypeScript compilation passes with no new errors after both tasks. Settings PUT endpoint enforces the three-key allowlist and rejects invalid values with appropriate HTTP status codes. Existing GET endpoints are unaffected.
</verification>

<success_criteria>
- src/validation/settings.ts exists with EDITABLE_SETTINGS, EditableSettingKey, settingValueSchemas exported
- PUT /api/settings/:key returns 400 for any key not in EDITABLE_SETTINGS
- PUT /api/settings/default_start_location returns 422 for values over 200 chars
- PUT /api/settings/discord_notifications_enabled returns 422 for values other than 'true'/'false'
- PUT /api/settings/meetup_description_template accepts values up to 5000 chars
- All existing valid settings writes continue to return 200
- npx tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-hardening-authorization-csrf-protection-and-input-validation/06-01-SUMMARY.md`
</output>
