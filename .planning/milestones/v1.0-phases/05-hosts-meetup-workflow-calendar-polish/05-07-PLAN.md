---
phase: 05-hosts-meetup-workflow-calendar-polish
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/events/event-form.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "When editing an existing event, the category filter dropdown shows the route's category pre-selected"
    - "Editing an event no longer requires the user to manually reselect the category to see the correct route in the list"
  artifacts:
    - path: "client/src/components/events/event-form.tsx"
      provides: "Category filter state initialized from existing event's route category"
      contains: "event?.route?.categoryId ?? null"
  key_links:
    - from: "client/src/components/events/event-form.tsx"
      to: "event.route.categoryId"
      via: "useState initializer"
      pattern: "useState.*categoryId"
---

<objective>
Fix the category filter prepopulation bug in EventForm. When editing an existing event the category filter always initializes to null, ignoring the route already on the event. This means the route dropdown shows no routes until the user manually picks a category.

Purpose: Eliminates a confusing UX regression where editing an event requires guessing and reselecting a category before the existing route is visible in the dropdown.
Output: One-line change to EventForm category filter initialization.
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed category filter state from existing event's route</name>
  <files>client/src/components/events/event-form.tsx</files>
  <action>
    On line 74, change:
      const [categoryFilter, setCategoryFilter] = useState&lt;number | null&gt;(null);
    to:
      const [categoryFilter, setCategoryFilter] = useState&lt;number | null&gt;(event?.route?.categoryId ?? null);

    That is the complete fix. No other changes needed.

    Verify the type is correct: event prop is typed as Event | undefined; the Event type has a route field with categoryId: number. The nullable coalescing to null matches the useState generic type parameter.
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    TypeScript compiles without errors. When editing an existing event, the category dropdown shows the route's category pre-selected and the route is visible in the filtered route list without any manual interaction.
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit passes with no new errors.
</verification>

<success_criteria>
CategoryFilter useState initializer reads event?.route?.categoryId ?? null. TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hosts-meetup-workflow-calendar-polish/05-07-SUMMARY.md`
</output>
