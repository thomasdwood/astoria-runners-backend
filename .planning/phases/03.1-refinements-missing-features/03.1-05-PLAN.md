---
phase: 03.1-refinements-missing-features
plan: 05
type: execute
wave: 4
depends_on: ["03.1-04"]
files_modified:
  - client/src/pages/admin/settings-page.tsx
  - client/src/components/categories/category-form.tsx
  - client/src/components/layout/admin-layout.tsx
  - client/src/App.tsx
  - client/src/components/routes/route-form.tsx
  - client/src/components/events/event-form.tsx
  - client/src/components/recurring/recurring-form.tsx
  - client/src/components/calendar/category-filter.tsx
autonomous: true
requirements: [categories, start-location, complex-patterns]

must_haves:
  truths:
    - "Admin settings page exists with categories management section and default start location section"
    - "Category form allows create/edit with name, color picker (from predefined palette), and icon (emoji input)"
    - "Category deletion shows confirmation and is blocked when category is in use"
    - "Route form uses category dropdown populated from API instead of hardcoded enum"
    - "Route and event forms include startLocation field with autocomplete from previously-used locations"
    - "Recurring form has structured dropdowns for frequency, day, and monthly position"
    - "Recurring form shows natural language preview and next 3 upcoming dates"
    - "Calendar category filter uses dynamic categories from API"
  artifacts:
    - path: "client/src/pages/admin/settings-page.tsx"
      provides: "Admin settings page with categories and default start location"
    - path: "client/src/components/categories/category-form.tsx"
      provides: "Category create/edit form with color picker and icon input"
    - path: "client/src/components/recurring/recurring-form.tsx"
      provides: "Updated recurring form with complex pattern support"
  key_links:
    - from: "client/src/pages/admin/settings-page.tsx"
      to: "client/src/hooks/use-categories.ts"
      via: "CRUD hooks"
      pattern: "useCategories|useCreateCategory|useDeleteCategory"
    - from: "client/src/pages/admin/settings-page.tsx"
      to: "client/src/hooks/use-settings.ts"
      via: "settings hooks"
      pattern: "useSettings|useUpdateSetting"
    - from: "client/src/App.tsx"
      to: "client/src/pages/admin/settings-page.tsx"
      via: "React Router route"
      pattern: "settings"
---

<objective>
Build the admin settings page (categories management + default start location), update all forms for new fields (categoryId, startLocation, complex recurring patterns), and update calendar category filter.

Purpose: The admin UI for managing categories and settings, plus updated forms that use the new data model.
Output: Settings page, updated route/event/recurring forms, dynamic calendar filter.
</objective>

<execution_context>
@/Users/tomwood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomwood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-refinements-missing-features/03.1-CONTEXT.md
@.planning/phases/03.1-refinements-missing-features/03.1-RESEARCH.md
@.planning/phases/03.1-refinements-missing-features/03.1-04-SUMMARY.md
@client/src/App.tsx
@client/src/components/layout/admin-layout.tsx
@client/src/components/routes/route-form.tsx
@client/src/components/events/event-form.tsx
@client/src/components/recurring/recurring-form.tsx
@client/src/components/calendar/category-filter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings page with categories management and default start location</name>
  <files>
    client/src/pages/admin/settings-page.tsx
    client/src/components/categories/category-form.tsx
    client/src/components/layout/admin-layout.tsx
    client/src/App.tsx
  </files>
  <action>
    1. Create `client/src/components/categories/category-form.tsx`:
       - React Hook Form with zod resolver (client uses zod v3)
       - Fields: name (text input), color (select dropdown from AVAILABLE_COLORS with color preview swatch), icon (text input for emoji — user types/pastes emoji)
       - Color select should show a colored dot next to each color name for visual reference
       - Accept optional `defaultValues` prop for edit mode
       - onSubmit calls create or update mutation
       - Small, focused form — use Dialog or inline editing

    2. Create `client/src/pages/admin/settings-page.tsx`:
       - Two sections separated by headers/cards:

       **Categories Section:**
       - Use useCategories() to list all categories
       - Display as a list/table: icon | name | color swatch | edit button | delete button
       - "Add Category" button opens CategoryForm in a Dialog
       - Edit button opens CategoryForm in Dialog with defaultValues
       - Delete button shows AlertDialog confirmation: "Delete {name}?" with warning. On confirm, call deleteCategory mutation. If 409 error (in_use), show toast: "Cannot delete — {N} routes use this category. Reassign them first."
       - Sort alphabetically (API already returns sorted)

       **Default Start Location Section:**
       - Show current default start location from useSettings()
       - Editable text input with "Save" button
       - On save, call useUpdateSetting with key='default_start_location'
       - Show success toast on save

       Page layout: consistent with other admin pages (PageHeader component, card containers)

    3. Update `client/src/components/layout/admin-layout.tsx`:
       - Add "Settings" nav item with gear/settings icon from lucide-react (Settings icon)
       - Place at bottom of nav list (after Recurring or at end)
       - Link to `/admin/settings`

    4. Update `client/src/App.tsx`:
       - Import SettingsPage
       - Add route: `<Route path="settings" element={<SettingsPage />} />` inside the admin Route group
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && cd client && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify settings page has both sections, category CRUD works, nav shows Settings link</manual>
  </verify>
  <done>
    - Settings page with categories list (CRUD) and default start location editor
    - Category form with name, color picker, icon input
    - Category delete blocked with toast when in use
    - Settings nav item in admin sidebar
    - Route added to App.tsx
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Update route, event, and recurring forms for new fields, update calendar filter</name>
  <files>
    client/src/components/routes/route-form.tsx
    client/src/components/events/event-form.tsx
    client/src/components/recurring/recurring-form.tsx
    client/src/components/calendar/category-filter.tsx
  </files>
  <action>
    1. Update `client/src/components/routes/route-form.tsx`:
       - Replace hardcoded category Select options with dynamic options from useCategories() hook
       - Select value should be `categoryId` (number) instead of category name string
       - Display: "icon name" in each option
       - Add `startLocation` text input field with autocomplete:
         - Use useLocationSuggestions() hook to get suggestions
         - Use useDefaultStartLocation() to pre-fill when creating new route (if startLocation is empty)
         - Implement autocomplete as: Popover + filtered list of suggestions matching typed text. Use Radix Popover (already installed). Show suggestions on focus/typing, filter as user types, select to fill input.
       - Make `endLocation` field not required (remove required indicator, allow empty submit)
       - Update form schema to use `categoryId: z.number()` instead of `category: z.enum(...)`

    2. Update `client/src/components/events/event-form.tsx`:
       - Add `startLocation` text input with same autocomplete pattern as route form
       - Pre-fill startLocation from default setting when creating new event
       - Make `endLocation` not required
       - Auto-copy: when endLocation is left empty on submit, copy startLocation value to endLocation in the form's onSubmit handler (client-side, per research anti-patterns recommendation)

    3. Update `client/src/components/recurring/recurring-form.tsx`:
       - Add structured dropdowns for complex patterns:
         - Frequency select: "Weekly" / "Every Other Week (Biweekly)" / "Monthly"
         - Day of week select: Monday through Sunday (already exists)
         - Monthly position select (only shown when frequency="monthly"): "1st" / "2nd" / "3rd" / "4th" / "Last"
       - Add natural language preview section below the dropdowns:
         - Call useRecurrencePreview() with current form values
         - Display: bold text like "Every 2nd Wednesday of the month"
         - Display: "Next dates:" followed by formatted list of 3 upcoming dates
         - Only show preview when frequency + dayOfWeek are selected (and bySetPos for monthly)
       - Add optional end date field (date picker input, nullable)
       - Add `startLocation` text input with autocomplete (same pattern as route/event forms)
       - Update form schema for new fields

    4. Update `client/src/components/calendar/category-filter.tsx`:
       - Replace hardcoded ALL_CATEGORIES with dynamic categories from useCategories() hook
       - Filter buttons/tabs should use category.name as label and CATEGORY_COLOR_MAP[category.color] for styling
       - Include "All" option that clears the filter
       - Emit category name (or id) as filter value
  </action>
  <verify>
    <automated>cd /Users/tomwood/Dev/web/astoria-runners && cd client && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify route form shows dynamic categories, recurring form shows pattern dropdowns with preview</manual>
  </verify>
  <done>
    - Route form uses dynamic category select from API
    - Route and event forms have startLocation with autocomplete
    - endLocation is optional everywhere with client-side auto-copy fallback
    - Recurring form has frequency/day/position dropdowns with natural language preview
    - Calendar filter uses dynamic categories
    - TypeScript compiles
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes with zero errors
- Settings page accessible at /admin/settings
- Category CRUD works from settings page
- All forms use new field structure
- Calendar filter uses dynamic categories
</verification>

<success_criteria>
- Admin settings page fully functional with category management and default start location
- All forms updated for new data model (categoryId, startLocation, complex recurring)
- Calendar category filter is dynamic
- Autocomplete for start location works across all forms
- Natural language recurring preview shown in recurring form
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-refinements-missing-features/03.1-05-SUMMARY.md`
</output>
