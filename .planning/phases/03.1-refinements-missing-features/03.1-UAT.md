---
status: testing
phase: 03.1-refinements-missing-features
source: 03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md, 03.1-03-SUMMARY.md, 03.1-04-SUMMARY.md, 03.1-05-SUMMARY.md, 03.1-06-SUMMARY.md
started: 2026-02-23T20:00:00Z
updated: 2026-02-24T04:53:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 14
name: Cancel Recurring Instance
expected: |
  On an upcoming recurring instance in admin events, click Cancel. The instance should become greyed out/strikethrough on the public calendar with a "Cancelled" badge, but still visible.
awaiting: user response

## Tests

### 1. Admin Settings Page - Categories Table
expected: Navigate to /admin/settings. You should see a Settings page with a Categories section showing a table of categories (Brewery Run, Coffee Run, Brunch Run, Weekend) each with a colored dot, icon emoji, and edit/delete buttons.
result: pass

### 2. Create Category
expected: On /admin/settings, click Add Category. A dialog with name, color dropdown (showing colored dot previews), and icon text input should appear. Submit to create a new category that appears in the table.
result: pass

### 3. Edit Category
expected: Click Edit on an existing category. The form pre-fills with current values. Change a field and save — the table updates immediately.
result: pass

### 4. Delete Category (In-Use Protection)
expected: Try to delete a category that has routes assigned to it. You should see an error/toast indicating the category is in use and cannot be deleted.
result: pass

### 5. Default Start Location Setting
expected: On /admin/settings, there should be a Default Start Location section showing "Astoria Park Track". You can edit it and save. The new value should persist on page reload.
result: pass

### 6. Route Form - Dynamic Category Select
expected: Go to create/edit a route. The category field should be a dropdown populated with categories from the API (not hardcoded). Selecting one should work.
result: pass

### 7. Route Form - Start Location Autocomplete
expected: On the route form, there should be a Start Location field that pre-fills with the default start location. Typing shows autocomplete suggestions from existing locations. End Location is optional.
result: pass

### 8. Event Form - Start Location with Default
expected: Go to create an event. Start Location should pre-fill with the default. Autocomplete suggestions appear when typing. End Location is optional and notes it defaults to start location.
result: pass

### 9. Recurring Template Form - Complex Patterns
expected: Go to create/edit a recurring template. You should see a Frequency selector (weekly/biweekly/monthly). Choosing "monthly" should reveal a position picker (1st, 2nd, 3rd, 4th, last). An optional end date field and start location autocomplete should be present.
result: pass

### 10. Recurring Template Preview
expected: When creating/editing a recurring template with frequency, day, and start time filled in, a preview section should show natural language text (e.g. "Every 2nd Wednesday of the month") plus the next 3 upcoming dates.
result: pass

### 11. Calendar - Dynamic Category Filter
expected: On the public calendar page, the category filter should show categories from the API (with colored dots) rather than hardcoded values. Filtering by category should show only events in that category.
result: pass

### 12. Calendar - Category Colors on Events
expected: Events on the calendar (month grid and list view) should show category-colored dots/badges that match the category's assigned color from settings.
result: pass

### 13. Admin Events - Recurring Instances
expected: On /admin/events, you should see both one-off events and upcoming recurring instances (next 8 weeks) mixed together sorted by date. Recurring instances should have a "Repeat" badge.
result: [pending]

### 14. Cancel Recurring Instance
expected: On an upcoming recurring instance in admin events, click Cancel. The instance should become greyed out/strikethrough on the public calendar with a "Cancelled" badge, but still visible.
result: [pending]

### 15. Delete Recurring Instance
expected: On an upcoming recurring instance in admin events, click Delete. The instance should completely disappear from the calendar (not just greyed out).
result: [pending]

### 16. Restore Cancelled Instance
expected: On a cancelled recurring instance in admin events, a Restore option should be available. Clicking it should remove the cancellation and the instance appears normal again on the calendar.
result: [pending]

### 17. Edit Recurring Instance (Exception)
expected: On an upcoming recurring instance in admin events, click Edit. This should open the event form pre-filled with the instance's route/time/notes. Saving creates a one-off exception event.
result: [pending]

### 18. Event Form Conflict Detection
expected: When creating an event on a date/time that overlaps with an existing recurring instance (same time-of-day window), a conflict dialog should appear offering "Replace" or "Keep Both".
result: [pending]

## Summary

total: 18
passed: 12
issues: 0
pending: 6
skipped: 0

## Gaps

[none yet]
